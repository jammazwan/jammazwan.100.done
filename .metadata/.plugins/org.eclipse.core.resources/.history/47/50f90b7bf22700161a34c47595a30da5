git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/petecarapetyan/${headers.projectname}.git
git push -u origin master

# "xyzCode" : ${headers.xyzCode}
# "name" : ${headers.name}
# projectname ${headers.projectname}
# projectName ${headers.projectName}
# desc: ${headers.desc}
# version ${headers.version}
# technology ${headers.technology}
# specialInstructions  ${headers.specialInstructions}
# pckg   ${headers.pckg}

  "xyzCode" : "xaa",
  "SingleObjectToJsonFile",
  "desc" : "Create an object and save it as json in a file",
  "version" : "0.1.0-SNAPSHOT",
  "technology" : "jackson, file",
  "specialInstructions" : "be careful not to ever do the wrong thing, it could be incorrect. Do the right thing instead",
  "pckg" : "jammazwan",
  "include" : "processor"