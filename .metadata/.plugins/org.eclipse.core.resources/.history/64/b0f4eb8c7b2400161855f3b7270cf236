package jammazwan.entity;

import static org.junit.Assert.*;

import org.apache.camel.test.spring.CamelSpringTestSupport;
import org.junit.Test;
import org.springframework.context.support.AbstractApplicationContext;
import org.springframework.context.support.ClassPathXmlApplicationContext;

public class XmplProjectTest extends CamelSpringTestSupport {

	@Test
	public void test() {
		String name = "ObjectToJsonFile";
		String desc = "Create an object and save it as json in a file";
		String version = "0.1.0-SNAPSHOT";
		String technology = "jackson";
		String specialInstructions = "";
		String pckg = "";
		XmplProject xmplProject = new XmplProject(name, desc, version, technology, specialInstructions, pckg);
		template.requestBody("direct:start", "some thing here doesn't matter what");
	}

	@Override
	protected AbstractApplicationContext createApplicationContext() {
		return new ClassPathXmlApplicationContext("META-INF/spring/camel-context.xml");

	}

}
